import { defineStore } from "pinia";

export const usePreloadStore = defineStore("usePreloadStore", {
  state: () => ({
    isLoading: false as boolean
  }), 
  getters: {
     
  },
  actions: {
  },
});
